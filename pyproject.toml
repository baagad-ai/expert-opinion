[tool.poetry]
name = "expert-opinion"
version = "0.1.0"
description = "Multi-expert parallel review GSD skill"
authors = ["baagad-ai"]
readme = "README.md"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
